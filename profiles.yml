default:
 target: dev
 outputs:
   dev:
     type: databricks
     method: http
     schema: "default"
     host: "{{ env_var('DBT_HOST') }}"
     http_path: "sql/protocolv1/o/{{ env_var('DBT_ORG_ID') }}/{{ env_var('DBT_CLUSTER_ID') }}"
     token: "{{ env_var('DBT_ACCESS_TOKEN') }}"
databricks:
  target: qna
  outputs:
    dev:
      type: databricks
      host: e2-demo-emea.cloud.databricks.com
      user: amos.gutman@databricks.com
      password: dapi76412b0917c515ac67bbf0fc83c784d6
      port: 443
      dbname: amos_dbt
      schema: amos_dbt
      threads: 4